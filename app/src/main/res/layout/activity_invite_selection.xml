<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:theme="@style/AppTheme.NoActionBar"
    tools:context="com.intelligentz.malchat.malchat.view.InviteSelectionActivity">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/flowericon"
        android:layout_margin="10dp"
        android:id="@+id/flower"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Click on Invite button to invite your friends to Mal Chat."
        android:textColor="@color/colorPrimaryDark"
        android:textSize="25dp"
        android:gravity="center"
        android:layout_centerInParent="true"
        android:id="@+id/looks"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="*Standard SMS charges will be applicable by your operator."
        android:textColor="@color/text_color"
        android:textSize="15dp"
        android:gravity="center"
        android:layout_below="@id/looks"
        android:id="@+id/charge"/>
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/skip"
        android:layout_alignParentBottom="true"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginBottom="10dp"
        android:id="@+id/skip"
        android:background="@android:color/transparent"
        android:textColor="@color/colorPrimary"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/curved_button_background"
        android:text="Invite"
        android:textAllCaps="false"
        android:textColor="@color/colorIcon"
        android:textStyle="bold"
        android:paddingTop="0dp"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginBottom="15dp"
        android:id="@+id/invite"
        android:layout_above="@id/skip"/>




</RelativeLayout>

